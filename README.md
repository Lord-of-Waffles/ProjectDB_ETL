# ProjectDB_ETL

SSIS project files are in the dir called ProjectDB_ETL

To install, first run the GenerateDatabase.sql script in SSMS to create the database, then you should be able to access the project.
